defmodule NewRelic.StatmanTest do
  use ExUnit.Case, async: false

  describe ".aggregate" do
    @tag :pending
    test "metrics"
    @tag :pending
    test "errors"
    @tag :pending
    test "start_time"
    @tag :pending
    test "end_time"
    @tag :pending
    test "empty data"
  end

end
